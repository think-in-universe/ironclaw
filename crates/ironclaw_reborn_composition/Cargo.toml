[package]
name = "ironclaw_reborn_composition"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
description = "Facade-shaped production composition root for IronClaw Reborn"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[package.metadata.ironclaw]
layer = "app"

[features]
default = []
# Use the single-process in-memory turn-state authority for the local-dev /
# hosted runtime family instead of the per-user `state.json` filesystem CAS
# store, even in libSQL/Postgres builds. This is the runtime-wedge fix: all of a
# user's concurrent turns (foreground + routines) coordinate in one in-process
# `InMemoryTurnStateStore` (no per-step CAS livelock) rather than contending on a
# single per-user turn-state document. Durability trade-off: in-flight compute
# turns are volatile across a process restart (short-lived, re-triggerable), but
# turns parked on a human gate (approval/auth) are persisted off the hot path via
# persist-on-block and rehydrated on startup, so a deploy does not silently drop
# an "Approve"-pending turn. Intended to be enabled by the
# hosted-single-tenant-volume deployment build; see
# `tools/ironclaw_stress/results/2026-06-30-turn-state-inmemory/` for the
# contention measurements that motivated it.
inmemory-turn-state = []
# Wire a host-managed LLM model gateway into the assembled runtime via
# ironclaw_runner's `LlmProviderModelGateway`. Off by default so this crate
# stays substrate-only when callers don't want a model gateway in the
# composed runtime. Mirrors `ironclaw_runner`'s own `root-llm-provider`
# feature shape (optional dep, `default-features = false`).
root-llm-provider = [
    "dep:ironclaw_llm",
    "ironclaw_llm/registry-provider-factory",
    "ironclaw_runner/root-llm-provider",
]
# Expose test-only helpers (mock LLM gateway, budget runtime accessors)
# so integration tests in this crate's `tests/` directory and downstream
# crates can drive `build_reborn_runtime` with a stub model gateway
# (via `RebornRuntimeInput::with_model_gateway_override`) and inspect the
# budget pipeline end-to-end. Enable with `cargo test --features
# test-support` (plus whatever feature surface the test crate also
# requires, e.g. `webui-v2-beta`).
test-support = [
    "ironclaw_channel_delivery/test-support",
    "ironclaw_host_runtime/test-support",
]
# Expose the narrow `extension_installation_store_for_migration` accessor for
# the v1→Reborn migration tool (`ironclaw_reborn_migration`). Ships zero bytes in
# a default production binary; mirrors the `test-support` substrate seams.
migration-support = []
# Compose the Reborn WebChat v2 HTTP gateway in this crate. Off by
# default; turning this on pulls in the v2 route crate plus the axum /
# tower-http middleware stack used by the `webui_serve` module. The
# standalone `ironclaw-reborn` binary opts in to call `serve_webui_v2`
# from its `serve` subcommand. Substrate-only callers (v1 `AppBuilder`,
# diagnostic harnesses) stay off the feature and carry no WebUI code.
webui-v2-beta = [
    # The WebChat v2 route surface + serve loop merged up into `ironclaw_webui`
    # (as its `webui_v2` module + `webui_serve`); this crate no longer depends on
    # that route crate. Composition keeps this feature to gate the WebUI facade,
    # identity, admin, and route-mount vocabulary it still owns.
    "dep:ironclaw_reborn_identity",
    "ironclaw_runner/webui-user-store",
    "libsql",
    "dep:axum",
    "dep:tower",
    "dep:tower-http",
    "dep:lru",
]
# Host-mounted Slack Events API ingress fragment for the Reborn ProductAdapter
# MVP. Off by default; host binaries opt in and mount it behind their Slack
# enablement/config checks.
slack-v2-host-beta = [
    "webui-v2-beta",
    "dep:ironclaw_slack_v2_adapter",
    "dep:ironclaw_wasm_product_adapters",
    "ironclaw_channel_host/webhook-serve",
    "ironclaw_product_workflow/storage",
]
# Host-mounted Telegram Bot API webhook ingress + pairing for the single
# user-visible `telegram` channel extension. Off by default; host binaries
# opt in and mount it behind their Telegram enablement/config checks.
telegram-v2-host-beta = [
    "webui-v2-beta",
    "dep:ironclaw_telegram_extension",
    "dep:ironclaw_wasm_product_adapters",
    "ironclaw_channel_host/webhook-serve",
    "ironclaw_product_workflow/storage",
]
openai-compat-beta = [
    "webui-v2-beta",
    "dep:ironclaw_reborn_openai_compat",
    "ironclaw_reborn_openai_compat/openai-compat-beta",
    "ironclaw_reborn_openai_compat/libsql",
    "ironclaw_product_workflow/libsql",
    "ironclaw_product_adapters/host-auth-mint",
]
libsql = [
    "dep:libsql",
    "ironclaw_filesystem/libsql",
    "ironclaw_host_runtime/libsql",
    "ironclaw_runner/filesystem-goal-store",
    "ironclaw_reborn_event_store/libsql",
    "ironclaw_resources/libsql",
    "ironclaw_run_state/libsql",
    "ironclaw_triggers/libsql",
]
postgres = [
    "dep:deadpool-postgres",
    "ironclaw_filesystem/postgres",
    "ironclaw_host_runtime/postgres",
    "ironclaw_runner/filesystem-goal-store",
    "ironclaw_runner/filesystem-local-trigger-access",
    "ironclaw_reborn_event_store/postgres",
    "ironclaw_resources/postgres",
    "ironclaw_run_state/postgres",
    "ironclaw_triggers/postgres",
]
[dependencies]
async-trait = "0.1"
base64 = "0.22"
chrono = "0.4"
deadpool-postgres = { version = "0.14", optional = true }
futures = "0.3"
ironclaw_auth = { path = "../ironclaw_auth" }
ironclaw_channel_host = { path = "../ironclaw_channel_host" }
ironclaw_channel_delivery = { path = "../ironclaw_channel_delivery" }
ironclaw_common = { path = "../ironclaw_common" }
ironclaw_capabilities = { path = "../ironclaw_capabilities" }
ironclaw_approvals = { path = "../ironclaw_approvals" }
ironclaw_attachments = { path = "../ironclaw_attachments" }
ironclaw_authorization = { path = "../ironclaw_authorization" }
ironclaw_conversations = { path = "../ironclaw_conversations" }
ironclaw_event_projections = { path = "../ironclaw_event_projections" }
ironclaw_event_streams = { path = "../ironclaw_event_streams" }
ironclaw_events = { path = "../ironclaw_events" }
ironclaw_extensions = { path = "../ironclaw_extensions" }
ironclaw_filesystem = { path = "../ironclaw_filesystem" }
ironclaw_first_party_extensions = { path = "../ironclaw_first_party_extensions" }
ironclaw_hooks = { path = "../ironclaw_hooks" }
ironclaw_host_api = { path = "../ironclaw_host_api" }
ironclaw_host_runtime = { path = "../ironclaw_host_runtime" }
ironclaw_llm = { path = "../ironclaw_llm", optional = true, default-features = false }
ironclaw_loop_host = { path = "../ironclaw_loop_host" }
ironclaw_memory = { path = "../ironclaw_memory" }
ironclaw_mcp = { path = "../ironclaw_mcp" }
ironclaw_network = { path = "../ironclaw_network" }
ironclaw_observability = { path = "../ironclaw_observability" }
ironclaw_outbound = { path = "../ironclaw_outbound" }
ironclaw_processes = { path = "../ironclaw_processes" }
ironclaw_product_adapters = { path = "../ironclaw_product_adapters" }
ironclaw_product_adapter_registry = { path = "../ironclaw_product_adapter_registry" }
ironclaw_product_workflow = { path = "../ironclaw_product_workflow" }
ironclaw_runner = { path = "../ironclaw_runner" }
ironclaw_reborn_config = { path = "../ironclaw_reborn_config" }
ironclaw_reborn_identity = { path = "../ironclaw_reborn_identity", optional = true }
ironclaw_reborn_event_store = { path = "../ironclaw_reborn_event_store" }
ironclaw_reborn_openai_compat = { path = "../ironclaw_reborn_openai_compat", optional = true }
ironclaw_reborn_traces = { path = "../ironclaw_reborn_traces" }
ironclaw_first_party_extension_ports = { path = "../ironclaw_first_party_extension_ports" }
ironclaw_resources = { path = "../ironclaw_resources" }
ironclaw_run_state = { path = "../ironclaw_run_state" }
ironclaw_runtime_policy = { path = "../ironclaw_runtime_policy" }
ironclaw_safety = { path = "../ironclaw_safety" }
ironclaw_secrets = { path = "../ironclaw_secrets" }
ironclaw_skills = { path = "../ironclaw_skills" }
ironclaw_slack_v2_adapter = { path = "../ironclaw_slack_v2_adapter", optional = true }
ironclaw_telegram_extension = { path = "../ironclaw_telegram_extension", optional = true }
ironclaw_threads = { path = "../ironclaw_threads" }
ironclaw_triggers = { path = "../ironclaw_triggers" }
ironclaw_projects = { path = "../ironclaw_projects" }
ironclaw_trust = { path = "../ironclaw_trust" }
ironclaw_turns = { path = "../ironclaw_turns" }
ironclaw_product_context = { path = "../ironclaw_product_context", version = "0.1.0" }
nix = { version = "0.31", default-features = false, features = ["process"] }
# Unpacking uploaded WASM-tool bundles for the WebUI "Install Tool" import flow.
zip = { version = "8", default-features = false, features = ["deflate"] }
# WebUI v2 route surface + static SPA bundle merged up into
# `ironclaw_webui`; no longer a composition dependency.
ironclaw_wasm_product_adapters = { path = "../ironclaw_wasm_product_adapters", optional = true }
libsql = { version = "0.9", optional = true, default-features = false, features = ["core", "replication", "remote", "tls"] }
libc = "0.2"
# axum + tower middleware stack used by the `webui_serve` module. Each
# entry is feature-gated so non-beta builds carry no HTTP surface code.
axum = { version = "0.8", optional = true }
tower = { version = "0.5", optional = true }
tower-http = { version = "0.6", optional = true, features = ["cors", "set-header", "limit", "catch-panic"] }
lru = { version = "0.18", optional = true }
fs4 = "1.1"
hex = "0.4.3"
rand = "0.10"
rust_decimal = { version = "1", features = ["serde", "serde-with-str"] }
secrecy = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
tempfile = "3"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread", "sync", "time"] }
tokio-util = { version = "0.7", features = ["rt"] }
toml = "1.1"
tracing = "0.1"
tracing-subscriber = "0.3"
url = "2"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
ironclaw_loop_host = { path = "../ironclaw_loop_host", features = ["test-support"] }
async-trait = "0.1"
chrono = "0.4"
hmac = "0.13"
# Enables the in-memory-backed outbound-state store constructor for tests only.
ironclaw_outbound = { path = "../ironclaw_outbound", features = ["test-support"] }
# Enables the in-memory-backed approval store constructors for tests only.
ironclaw_approvals = { path = "../ironclaw_approvals", features = ["test-support"] }
# Enables the in-memory-backed capability-lease store constructor for tests only.
ironclaw_authorization = { path = "../ironclaw_authorization", features = ["test-support"] }
# Enables the in-memory-backed process store constructors for tests only.
ironclaw_processes = { path = "../ironclaw_processes", features = ["test-support"] }
# Enables the in-memory-backed run-state/approval store constructors for tests only.
ironclaw_run_state = { path = "../ironclaw_run_state", features = ["test-support"] }
# Enables the in-memory-backed budget-gate store constructor for tests only.
ironclaw_resources = { path = "../ironclaw_resources", features = ["test-support"] }
http = "1"
http-body-util = "0.1"
# Admin-user-management e2e test drives the composed app with a real session
# authenticator so a minted API token can be validated (login-with-token).
# `slack-v2-host-beta` is enabled because composition's own WebUI integration
# tests (`tests/webui_v2_product_auth*.rs`) exercise
# `#[cfg(feature = "slack-v2-host-beta")]` blocks that call the
# `WebuiServeConfig::with_slack_*` setters — which moved into this ingress crate
# and are gated on the ingress slack feature. Cargo features cannot reach a
# dev-dependency, so the dev-dep must request the feature directly.
ironclaw_webui = { path = "../ironclaw_webui", features = ["slack-v2-host-beta"] }
secrecy = { version = "0.10", features = ["serde"] }
ironclaw_common = { path = "../ironclaw_common" }
ironclaw_host_api = { path = "../ironclaw_host_api" }
ironclaw_host_runtime = { path = "../ironclaw_host_runtime", features = ["test-support"] }
ironclaw_conversations = { path = "../ironclaw_conversations" }
ironclaw_reborn_composition = { path = ".", features = ["test-support"] }
ironclaw_threads = { path = "../ironclaw_threads" }
ironclaw_turns = { path = "../ironclaw_turns" }
ironclaw_product_workflow = { path = "../ironclaw_product_workflow", features = ["test-support"] }
ironclaw_product_adapters = { path = "../ironclaw_product_adapters" }
rust_decimal = { version = "1", features = ["serde", "serde-with-str"] }
rust_decimal_macros = "1"
sha2 = "0.11"
tempfile = "3"
testcontainers-modules = { version = "0.12", features = ["postgres"] }
tokio = { version = "1", features = ["macros", "net", "rt", "rt-multi-thread", "time"] }
# Real WebSocket client for the v2 stream-events-ws caller-level test.
# Oneshot via `tower::ServiceExt::oneshot` cannot complete a true WS
# upgrade (no underlying TCP stream → axum's WebSocketUpgrade extractor
# returns 426), so the happy-path test needs a bound listener and a
# proper WS client.
tokio-tungstenite = { version = "0.29", default-features = false, features = ["connect"] }
tower = { version = "0.5", features = ["util"] }
tracing-test = "0.2"

[[test]]
name = "production_runtime_automations"
required-features = ["libsql"]

[[test]]
name = "resource_governor_libsql_contract"
required-features = ["libsql"]

[build-dependencies]
ironclaw_skills = { path = "../ironclaw_skills" }
serde_json = "1"

[lints]
workspace = true
